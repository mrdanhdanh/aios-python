[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "aios-sdk"
version = "1.0.0"
description = "Public Python SDK for AIOS"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
