# AIOS SDK — TypeScript

> **Trạng thái: chưa code (stub)** — sẽ được xây dựng trong M1/M2 theo plan.

SDK TypeScript cho VS Code extension + dashboard, và viết Tool bằng TS.
Client kết nối backend qua REST/WebSocket (FastAPI).

## Roadmap

- [ ] API client (REST + WebSocket) — M3
- [ ] Tool authoring bằng TS — M4

Xem chi tiết: [`docs/PLAN.md`](../docs/PLAN.md)
