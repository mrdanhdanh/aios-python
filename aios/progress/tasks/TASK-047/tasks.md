# TASK-047 — Implementation checklist

- [x] Spec + critique ×2 resolve.
- [x] Review pre-implement.
- [ ] `ecosystem/devkit.py` (DevKit.create_scaffold, 5 templates).
- [ ] CLI `aiagent plugin create <kind> <name> [--dir]`.
- [ ] `tests/test_ecosystem_devkit.py` (structure + compile + yaml round-trip + deterministic + no-overwrite).
- [ ] Regression + progress + commit.

# Review pre-implement
**APPROVED có điều kiện**: stateless, deterministic, no-overwrite, stub compile, yaml round-trip.
