{
  "name": "yuniebel-phaser",
  "version": "1.0.0",
  "description": "Yuniebel's Cat — bản Phaser 4 (Vite) của webgame 2D pixel (TASK-081). Migrate scene/dialogue từ bản vanilla games/yuniebel/ — vendor core/sprites/audio byte-identical, 1 GameScene Phaser re-render bg texture mỗi frame, UI DOM overlay.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "gen:sprites": "node tools/gen-sprites.mjs",
    "pretest": "node tools/gen-sprites.mjs",
    "pretest:visual": "node tools/gen-sprites.mjs",
    "test": "vitest run && playwright test",
    "test:core": "vitest run test/core.test.js",
    "test:smoke": "vitest run test/smoke.test.js",
    "test:e2e": "playwright test",
    "test:visual": "playwright test visual"
  },
  "keywords": [
    "phaser",
    "game",
    "pixel-art",
    "yuniebel"
  ],
  "license": "ISC",
  "dependencies": {
    "phaser": "^4.2.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "jsdom": "^25.0.1",
    "vite": "^6.0.0",
    "vitest": "^2.1.8"
  }
}
